---
title: "9. 什么是多步推理? Agent 如何实现?"
source: https://www.yuque.com/u28128023/mk3u4m/crpzogrpvurwsbnn
exported_at: 2026-07-27
---

# 9. 什么是多步推理? Agent 如何实现?

## 知识点讲解

### 1. 核心概念

多步推理，说白了就是"想清楚再说"——把一个复杂问题拆成若干步，每步基于上一步的结果继续推进，最终得出答案。

为什么需要多步？因为 LLM 单步推理在简单问题上够用，但遇到需要多条信息交叉验证、需要中途调用工具、需要回溯修正的问题，一步到位就会出错。多步推理的本质是给 LLM 装上"中间草稿纸"。

Agent 实现多步推理最核心的机制是**思维链（Chain of Thought）+ 工具调用的结合**：LLM 输出中间推理过程，工具处理 LLM 算不准的部分（计算、查询），每步结果写入上下文，驱动下一步。

### 2. 技术细节

#### 2.1 多步推理的几种模式

**① 顺序推理（最简单）**

步骤 1 → 步骤 2 → 步骤 3 → 答案。每步依赖上一步。ReAct 就是这种——Thought → Action → Observation 循环往复。

**② 分解-合并推理**

把问题拆成互相独立的子问题，分别解决，最后合并。适合子问题之间没有依赖的情况，理论上可以并行，效率更高。

**③ 反思-修正推理**

先给一个初步答案，再让 LLM 检查自己的推理有没有问题，发现错误就修正。这是 Self-Reflection 类方法的核心思路。

**④ 树搜索推理（最重量级）**

在每个决策节点展开多个候选路径，评估分数，保留最优的继续探索。Tree of Thoughts 和 LATS 走这条路——能处理需要回溯的复杂任务，但 token 消耗极大。

#### 2.2 推理链的工程问题

多步推理看起来简单，落地有几个坑：

**推理链断裂**：上一步的 observation 没有正确注入下一步的 prompt，导致后续推理"忘了"前面发生了什么。解决办法是在每步 prompt 里明确包含完整的历史推理链。

**中间步骤幻觉**：某一步 LLM 推理出来的"中间结论"本身就是错的，后续推理基于错误前提继续，越跑越偏。关键步骤（尤其是数值计算、事实核查）要走工具而不是靠 LLM 自己算。

**步骤数膨胀**：Agent 绕弯路，10 步能完成的任务跑了 30 步。这既浪费 token，也意味着规划有问题。要有 max\_steps 硬上限，同时评估"步数效率"（实际步数 vs 最优步数）。

#### 2.3 LLM 推理 vs 工具辅助推理的边界

| 适合 LLM 自己推理 | 适合交给工具 |
| --- | --- |
| 逻辑推断、因果分析 | 精确数值计算 |
| 语义理解、意图识别 | 实时数据查询 |
| 方案生成、文本写作 | 代码执行、文件操作 |
| 评估和判断 | 数据库 CRUD |

这条边界很重要：LLM 在推理类任务上很强，在精确计算上很弱。把计算交给工具、推理留给 LLM，是多步推理系统的基本设计原则。

### 3. 对比与拓展

#### 3.1 多步推理的成本考量

多步推理比单步推理贵很多。每步一次 LLM 调用，10 步就是 10 次调用，加上 token 随推理链增长还越来越多。所以高级别的树搜索推理（ToT、LATS）一般只用在答案可验证的高价值任务上（代码生成、数学）；普通任务用顺序推理就够了。

## 面试怎么答

> 多步推理是把复杂问题分成若干步骤、逐步推进得出答案的过程。它存在的原因是 LLM 单步推理对复杂问题不够——既可能推理出错，又没办法在中间调工具获取数据。多步推理相当于给 LLM 一张"草稿纸"，中间结果可以检查、可以修正。
> 
> Agent 实现多步推理的核心是思维链 + 工具调用的结合：LLM 负责推理和决策，计算/查询类的步骤交给工具处理，每步结果写入上下文，驱动下一步推理。
> 
> 有几个工程上的注意点：LLM 算不准数值，计算类步骤必须走工具；推理链要完整注入后续 prompt，否则 Agent 会"失忆"；要有 max\_steps 上限防止无限循环；高价值复杂任务可以用树搜索（ToT/LATS），普通任务顺序推理就够了，不要为了复杂而复杂。

## 高频追问 & 加分点

-   **Q1：Chain of Thought 和多步推理是一回事吗？** CoT 是多步推理的一种实现方式——让 LLM 显式写出推理过程（"先……然后……因此……"），通过这种结构化输出提高准确率。多步推理是更宽泛的概念，包括 ReAct、Plan-and-Execute、树搜索等多种范式，CoT 可以嵌套在其中任何一种里。

-   **Q2：怎么判断一个任务需不需要多步推理？** 两个判断维度：①这个问题 LLM 一步能不能答对（做几个测试）；②需不需要中途调工具获取数据。两者任意一个是"是"，就需要多步推理；两个都是"否"，直接单步更省钱省时间。

-   **Q3：多步推理的中间步骤出错了怎么办？** 取决于在哪步发现的。如果是工具返回了错误（API 调用失败等），立即重试或换工具。如果是后续推理发现前面的中间结论有问题，需要回溯——简单场景回退到出错那步重做，复杂场景用 Self-Reflection 让 LLM 自评然后修正。

-   **Q4：多步推理的 token 消耗怎么控制？** 三个方向：①步骤数控制（规划时就要求子任务数不超过 N）；②上下文压缩（推理链太长时摘要压缩历史步骤）；③模型分级（推理步骤用小模型，只在关键决策节点用大模型）。

-   **Q5：Self-Reflection 是怎么回事？** 让 Agent 在完成一步或完成全部任务后，用另一次 LLM 调用检查自己的推理和结果——"我的推理有没有逻辑漏洞？结果有没有满足原始目标？"发现问题就修正。这是对抗中间步骤幻觉的有效手段，代价是多一次 LLM 调用。
