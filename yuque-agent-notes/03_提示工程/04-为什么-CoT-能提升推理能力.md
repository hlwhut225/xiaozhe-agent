---
title: "4. 为什么 CoT 能提升推理能力？"
source: https://www.yuque.com/u28128023/mk3u4m/uykmxnryka98biz2
exported_at: 2026-07-27
---

# 4. 为什么 CoT 能提升推理能力？

## 知识点讲解

### 1. 核心概念

CoT 为什么管用，从直觉到机制有几层解释，理解它能帮你在更多场景正确使用。

最直觉的解释：LLM 是自回归生成的，每个 Token 的生成都依赖前面所有的 Token。写出推理步骤，就是在为下一步的推理准备"垫脚石"——中间步骤是下一步推理的上下文，而不只是最终答案的"装饰"。

类比人的思维过程：你解一道数学题，把每步算式写下来，后面就不容易算错。CoT 给模型提供的，就是这个"写下来"的空间。

### 2. 技术细节

#### 2.1 三个层面的机制解释

**① 计算空间充足**

Transformer 每次生成一个 Token，这一步的计算量是固定的。对于多步推理任务，如果强迫模型在一个 Token 里"想完所有步骤"，计算容量不够。CoT 把多步推理展开成多个 Token 的生成序列，每步都有足够的计算空间。

**② 约束传播**

中间步骤输出后，它成为后续步骤的 context。如果中间步骤有错误，后续步骤的推理会受到约束，更容易"检测"到矛盾并自我纠正（不是总能，但比直接跳到答案好）。

**③ 上下文学习强化**

Few-shot CoT 里的示例展示了"正确的推理风格"——分析条件、建立方程、逐步求解。模型通过上下文学习模仿这个风格，迁移到新问题上。这种风格学习比"记住答案"更稳定。

#### 2.2 实验数据参考

-   **历史基准（Wei et al. 2022, PaLM 540B）**：GSM8K 直接回答 ~60%，CoT 提升到 ~90%——这是 CoT 论文最有名的数据，但来自 2022 年的基础模型

-   **当前格局（2024-2026 年的前沿模型）**：GPT-4o / Claude 3.5+ / Gemini 2.0 在 GSM8K 上 zero-shot 已经 95%+，**CoT 的"绝对提升"被压缩了，但在更难的 benchmark（MATH、AIME、ARC-AGI）上 CoT 依然有显著价值**

-   MMLU 推理子集：CoT 仍能提升约 5-15 个百分点

-   收益规律：模型越强、任务越难，CoT 越管用；前沿模型在简单任务上"用不用 CoT 差不多"

#### 2.3 CoT 对什么任务没用

CoT 对简单事实查询几乎没有帮助，有时候还有害——模型可能在推理步骤里引入本来不存在的中间误差。判断标准：**任务是否真的需要多步推理**？不需要的话不要用 CoT。

### 3. 对比与拓展

CoT 思想衍生的方向：

| 技术 | 核心思想 | 适用场景 |
| --- | --- | --- |
| CoT | 单条推理链 | 通用推理 |
| Self-Consistency | 多条链投票 | 需要高准确率 |
| Tree of Thought | 多路径探索 | 需要回溯 |
| Scratchpad | 用草稿本 | 长计算任务 |
| Program of Thoughts | 生成代码执行 | 数学计算 |

## 面试怎么答

> CoT 提升推理能力的核心原因：LLM 是自回归生成的，写出中间推理步骤相当于给模型提供了"计算草稿纸"——每一步的输出成为下一步的上下文，多步推理被分解成多次局部计算，每次都有足够的计算容量。
> 
> 从约束角度看，中间步骤的存在会对后续推理形成逻辑约束。如果中间步骤有问题，后续步骤的矛盾更容易被"感知"到，有时候模型会自我纠正。
> 
> 数据上要分时代讲：CoT 论文（2022 年 PaLM 540B）在 GSM8K 上是 60% → 90%，这个对比在当年很震撼。但**到 2026 年的前沿模型，GSM8K zero-shot 已经 95%+，CoT 的"绝对提升空间"被压缩了**——所以现在面试官如果问"CoT 还有没有价值"，回答应该是"在更难的任务（MATH、AIME、ARC-AGI、长链路推理）上依然显著，简单任务上反而没必要"。CoT 对模型能力的依赖也变了，**指令微调后的小模型（如 Llama-3-8B-Instruct、Qwen2.5-7B-Instruct）现在已经能用好 CoT**，2022 年那条"<13B 没用"的结论是针对 base model 的，已经过时了。

## 高频追问 & 加分点

-   **Q1：CoT 对小模型有效吗？** 得分**基础模型**和**指令模型**两类讲。**Base model 小尺寸**（如 LLaMA 7B base）确实如 2022 年 CoT 论文所说，CoT 收益很弱甚至有害——推理步骤自己就错。但 **2024 年后的 Instruct 小模型**（Llama-3-8B-Instruct、Qwen2.5-7B-Instruct、Phi-4）已经能很好地利用 CoT，在 GSM8K 上 8B 级 Instruct 模型 + CoT 通常能跑到 80%+。"<13B 模型 CoT 无效"是过时结论，回答时点明这个时代差是加分项。

-   **Q2："Let's think step by step"这句话为什么有效？** 这是 Zero-shot CoT 的触发短语，在训练数据里"逐步推理"的示例通常伴随着这类措辞。模型学到了"看到这种表达后，应该展示推理步骤"的模式。不是魔法，是统计规律。

-   **Q3：CoT 增加了多少计算成本？** 主要是输出 Token 增加，通常 2-5 倍。从成本控制角度，可以只在真正需要推理的步骤用 CoT，其他简单步骤不用。或者用"压缩 CoT"：让模型在内部推理但不输出过程（System 2 Thinking 的变体）。

-   **Q4：Program of Thoughts 是 CoT 的变体吗？** 是的。PoT 让模型生成代码（而不是自然语言推理步骤），然后执行代码得到答案。对于数学计算类任务，代码是精确的，执行结果不会有计算错误——这比自然语言 CoT 更可靠，因为 LLM 的算术不如代码执行器准确。
