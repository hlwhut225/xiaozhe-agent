---
title: "3. 什么是思维链（Chain of Thought）？"
source: https://www.yuque.com/u28128023/mk3u4m/rsb4o460hgqvdggv
exported_at: 2026-07-27
---

# 3. 什么是思维链（Chain of Thought）？

## 知识点讲解

### 1. 核心概念

思维链（Chain of Thought，CoT）是让 LLM 在给出答案之前先写出推理步骤的提示技术。不是直接问"答案是什么"，而是让模型"边想边说"，把中间推理过程展示出来。

一句经典的触发语就够了："让我们一步步思考。"（"Let's think step by step."）这是 2022 年谷歌论文里证明有效的 Zero-shot CoT 触发方式。

CoT 特别适合数学、逻辑推理、多步规划这类任务——在这些场景上，准确率提升通常是 20-50%，有时候更多。

### 2. 技术细节

#### 2.1 CoT 的触发方式

| 方式 | 写法 | 适用 |
| --- | --- | --- |
| Zero-shot CoT | "请一步步思考..." | 快速上手，通用 |
| Few-shot CoT | 示例里展示推理过程 | 效果更稳定 |
| 角色设定 CoT | "你是一个严谨的数学老师，请..." | 风格控制 |
| 结构化 CoT | 明确列出步骤框架 | 流程固定的任务 |

#### 2.2 CoT 的适用场景

有明显帮助的：

-   数学计算（多步运算）

-   逻辑推理（三段论、条件判断）

-   代码分析（逐步 debug）

-   复杂决策（多因素权衡）

帮助不大的：

-   简单事实查询（"法国首都是哪"）

-   单步分类（"这句话正面还是负面"）

-   创意写作（中间过程不重要）

#### 2.3 CoT 变体

-   **Tree of Thought（ToT）**：不只一条链，而是让模型探索多条推理路径，像树一样展开，然后选最优分支

-   **Self-Consistency**：同一问题跑多条 CoT 链，投票选出最多的答案

-   **Least-to-Most**：先解决简单子问题，再用子问题的答案解决更复杂的

## 代码示例

## 面试怎么答

> Chain of Thought 是让模型在给出答案前把推理步骤写出来的技术。触发方式最简单的就是在问题后加"请一步步思考"，或者在 Few-shot 示例里展示推理过程。
> 
> 为什么有效？有几个原因：模型是自回归生成的，写出中间步骤相当于给自己搭了"脚手架"，每一步的输出作为下一步的输入，避免了跳步出错；而且每个中间步骤都对后续的推理形成约束，一步错了后续可以被"纠正"。
> 
> 适合的场景：数学、逻辑推理、多步规划、代码分析——这些任务在直接回答时容易跳步出错。不适合简单事实查询，那种情况 CoT 反而是浪费 Token。

## 高频追问 & 加分点

-   **Q1：CoT 会增加多少成本？** 推理步骤会增加输出 Token 数，通常是原来的 2-5 倍。但对于推理任务，这个投入是值得的——省掉了多轮重试的成本。如果成本是瓶颈，可以只在关键步骤用 CoT，简单步骤直接答。

-   **Q2：Zero-shot CoT vs Few-shot CoT 怎么选？** 先试 Zero-shot CoT（加"请一步步思考"），简单快速；如果效果不稳定，上 Few-shot CoT——提供带完整推理步骤的示例，效果更可靠。Few-shot CoT 的示例选择很关键，要展示正确的推理模式，不是随便写几步。

-   **Q3：CoT 能防止幻觉吗？** 有一定帮助，但不能完全防止。中间步骤让推理更透明，错误更容易被发现；但如果模型在某一步就用了错误的知识，后面还是会错。CoT 对推理错误有帮助，对知识错误帮助有限，知识错误要靠 RAG。

-   **Q4：Tree of Thought 是 CoT 的升级版吗？** 可以这么理解。ToT 不止维护一条推理链，而是让模型探索多个可能的推理路径，像一棵树展开，然后由评估器选最优分支。适合需要回溯和探索的任务（代码 debug、数学难题），但 Token 消耗成倍增加。
