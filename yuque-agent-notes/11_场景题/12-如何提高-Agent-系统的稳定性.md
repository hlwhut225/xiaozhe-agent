---
title: "12. 如何提高 Agent 系统的稳定性"
source: https://www.yuque.com/u28128023/mk3u4m/puh9ir9uek1crav3
exported_at: 2026-07-27
---

# 12. 如何提高 Agent 系统的稳定性

## 知识点讲解

### 1. 核心概念

Agent 稳定性，说白了就是：**出了问题能继续跑，跑不下去能优雅降级，不能让一个点的故障把整个系统拖垮**。

Agent 系统的故障风险来自四类：

-   **模型风险**：LLM 推理错误、幻觉、超时

-   **工具风险**：工具函数 bug、第三方服务不可用

-   **外部风险**：网络抖动、API 限流、服务器故障

-   **逻辑风险**：Agent 陷入死循环、无限重试、资源耗尽

每类风险的处理方式不同，不能用同一个"重试"解决所有问题。

### 2. 技术细节

#### 2.1 错误分类与差异化处理

| 错误类型 | 例子 | 处理策略 |
| --- | --- | --- |
| 临时错误 | 网络超时、API 限流 | 指数退避重试 |
| 持续错误 | API 密钥失效、服务下线 | 立即降级，不要重试 |
| 逻辑错误 | 参数类型错误、权限拒绝 | 直接失败，不重试，上报 |
| 资源错误 | OOM、磁盘满 | 拒绝新请求，等待恢复 |

临时错误才值得重试，逻辑错误重试 10 次也没用，还浪费资源。

#### 2.2 三个核心机制

**重试 + 指数退避**：首次失败等 1 秒，第二次 2 秒，第三次 4 秒。避免在服务端压力大时大量请求同时重试，把服务打垮。加抖动（jitter）防止雪崩。

**断路器（Circuit Breaker）**：连续失败 N 次，自动"断路"——停止向该服务发请求，直接走降级。等一段时间后进入"半开"状态，允许少量请求探测服务是否恢复。恢复了就重新开路。

**降级策略**：服务不可用时不要直接失败，有备用方案：返回缓存的上次结果、走简化版本（不调工具，只靠模型知识）、返回默认值并告知用户"功能暂时受限"。

#### 2.3 防止死循环

Agent 最容易犯的"稳定性错误"：在工具调用失败时不断重试，导致无限循环。

防护措施：

-   全局最大步数限制（比如 20 步）

-   相同工具调用重复次数限制（同一个工具 + 相同参数，重复 3 次就停）

-   总耗时超过阈值就中止（比如 30 秒）

## 代码示例

## 面试怎么答

> Agent 稳定性的关键是分层防护，不同类型的错误用不同的手段处理。
> 
> 临时错误（网络抖动、限流）用指数退避重试，等的时间越来越长，加点随机抖动防止"集体重试"打垮服务。持续错误不重试，直接降级——LLM API 不可用，就走规则兜底或返回友好提示，不要让用户等着。
> 
> 断路器是生产系统必须有的机制：连续失败 N 次就断路，停止向故障服务发请求，等一段时间后小流量探测是否恢复。这能防止故障服务在压力下雪崩。
> 
> 防死循环也很重要：设全局最大步数、相同工具重复调用次数限制、总耗时上限，任何一个触发就中止。这些都是硬约束，不能靠模型自己判断什么时候停。

## 高频追问 & 加分点

-   **Q1：重试次数设多少合适？** 通常 2-3 次，不要超过 5 次。重试次数多了，在服务端问题期间会产生大量积压请求，加重问题。更重要的是重试间隔：第一次 1s，第二次 2s，第三次 4s，加 10% 随机抖动。如果是 API 限流（429），第一次重试可以直接等 Rate Limit 响应头里给出的等待时间。

-   **Q2：降级到什么程度才算合适？** 降级目标是"可用"而不是"好用"。层级上：全功能 > 简化功能（不调工具，只靠模型）> 缓存结果 > 友好提示。能返回有价值的内容就返回，实在不行就返回"服务暂时不可用，请稍后重试"。绝对不能返回空白或崩溃报错——那对用户体验伤害最大。

-   **Q3：如何测试 Agent 的稳定性？** 故障注入测试（Chaos Engineering）：在测试环境里模拟 API 超时、随机错误、工具失败，看系统怎么响应。验证点：① 重试是否按预期退避；② 断路器是否正确触发；③ 降级响应是否友好；④ 没有无限循环；⑤ 日志是否完整记录了故障链路。定期跑这些测试，不要等上了生产才发现问题。

-   **Q4：多个 Agent 组成的系统，某一个挂了怎么处理？** 依赖关系是关键。如果挂的 Agent 是可选的（锦上添花），跳过它继续执行；如果是关键路径上的，触发整体降级。Orchestrator 要维护每个 Worker 的状态，Worker 失败后能决定是重试、替换其他 Agent、还是整体回退。不能让一个 Worker 的失败悄悄消失，必须上报到 Orchestrator 层处理。
