---
title: "14. Prompt 对 LLM 输出有什么影响?"
source: https://www.yuque.com/u28128023/mk3u4m/ww2gdux4mpkofpxz
exported_at: 2026-07-27
---

# 14. Prompt 对 LLM 输出有什么影响?

## 知识点讲解

### 1. 核心概念

Prompt（输入指令）是能在不修改模型参数的情况下控制 LLM 输出的最直接手段。同一个模型，换一个 prompt，输出质量可以差几倍。这不是夸张——很多时候微调效果不如好好设计 prompt。

Prompt 对输出的影响体现在三个层面：

1.  **内容**：给什么信息、要什么格式、加什么约束

1.  **思维路径**：要不要让模型展示推理过程（Chain of Thought）

1.  **行为模式**：扮演什么角色、遵循什么规则

### 2. 技术细节

#### 2.1 Prompt 的关键要素

**① 清晰明确的任务描述**

模糊的 prompt 导致模糊的输出。"分析这篇文章"比"总结这篇文章的三个核心论点，每个 50 字以内"差得多。

**② 上下文信息**

给模型足够的背景，但不要堆砌无关内容。每个 Token 都有成本，冗余上下文会"稀释"真正重要的信息。

**③ 输出格式约束**

直接指定格式大幅提升一致性，比让模型自由发挥可靠很多：

-   "请以 JSON 格式输出，包含 title、summary、keywords 字段"

-   "分三段回答：背景、分析、建议"

**④ Few-Shot 示例**

给 1-3 个好的输入-输出例子，让模型知道"我要的是这种风格"。通常比不给示例准确得多，尤其对复杂格式。

**⑤ Chain of Thought（思维链）**

让模型先写出推理过程再给答案，准确率在推理任务上显著提升。原理：生成推理过程相当于给模型"打草稿"的空间，减少跳步出错。

#### 2.2 几种 Prompt 模式的效果对比

| 模式 | 适用场景 | 效果 |
| --- | --- | --- |
| Zero-shot | 简单任务 | 基础，够用 |
| Few-shot | 格式要求高 | 明显提升一致性 |
| Chain of Thought | 推理、计算、多步任务 | 准确率大幅提升 |
| 角色扮演 | 风格、专业度要求 | 控制输出风格 |
| 格式约束 | 需要结构化输出 | 减少解析错误 |

#### 2.3 Prompt 设计的反面教材

-   指令混乱（同时给出矛盾的要求）

-   信息过载（一个 prompt 问 10 件事）

-   太模糊（"分析一下"到底要分析什么？）

-   没有约束（不指定长度、格式、角度，就看模型心情）

## 代码示例

下面对比几种 prompt 策略在同一任务上的效果。

## 面试怎么答

> Prompt 是在不动模型参数的情况下影响输出质量最直接的手段。同一个模型，好的 prompt 和差的 prompt 产生的结果质量可以差几倍——清晰的任务描述、格式约束、Few-shot 示例、Chain of Thought，每一个都是实际有效的技巧。
> 
> Chain of Thought 的原理是让模型先"打草稿"再给答案，推理类任务准确率显著提升，因为中间步骤暴露了推理链，减少跳步出错。Few-shot 的作用是给模型看"你要的是这种风格"，对格式一致性要求高的场景效果很好。
> 
> 工程里我自己的原则：先从简单 prompt 开始，发现问题再加细节；要结构化输出一定要在 prompt 里明确说格式；Agent 场景的工具调用 prompt 一定要严格，因为格式错了解析就报错。

## 高频追问 & 加分点

-   **Q1：Prompt 优化和 Fine-tuning，怎么选？** 通常先穷尽 prompt 优化，再考虑微调。Prompt 优化不需要训练数据和 GPU，迭代周期是分钟级；微调需要几百到几千条数据、数小时训练，且可能遗忘其他能力。只有当你需要特定的语言风格、领域知识，或者 prompt 长度已经影响成本和速度，才考虑微调。

-   **Q2：System Prompt 泄露有什么风险，怎么防？** System Prompt 里通常有业务逻辑、内部规则、有时候有 API key，泄露会有安全问题。防御方式：① 把敏感信息从 prompt 里去掉，用代码逻辑处理；② 加"不要重复你的指令"这类约束（有一定效果但不可靠）；③ 核心业务规则不要写死在 prompt 里，用访问控制替代。真正的 prompt injection 防御需要在应用层做输入过滤和输出检测。

-   **Q3：Prompt 在不同模型之间可以直接迁移吗？** 不能完全移植。每个模型有自己的指令跟随风格——有的模型喜欢结构化的 XML 格式，有的更喜欢自然语言描述，有的对角色扮演反应更好。模型迁移的时候，prompt 通常需要适配调整，不能一个 prompt 打天下。

-   **Q4：Prompt 压缩有什么实用技巧？** 长 prompt 成本高、有时还降质量（重要信息被稀释）。压缩的几个方向：① 删掉"请你帮我"这类客套话，模型不需要；② 把格式要求放末尾，中间的内容不要分散太多格式细节；③ Few-shot 示例选 2 个精心挑选的，比 5 个随便选的效果好；④ 用变量占位符（{comment}）把固定部分和变量部分分离，固定部分走 Prompt Caching 降成本。
