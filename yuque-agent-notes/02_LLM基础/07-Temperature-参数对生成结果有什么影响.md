---
title: "7. Temperature 参数对生成结果有什么影响?"
source: https://www.yuque.com/u28128023/mk3u4m/muxrwvfi2i7p91uq
exported_at: 2026-07-27
---

# 7. Temperature 参数对生成结果有什么影响?

## 知识点讲解

### 1. 核心概念

Temperature（温度）是控制 LLM 生成多样性的采样参数。它在 softmax 之前对 logits 做缩放：

-   **T < 1**：logits 放大，概率分布更集中，高概率 Token 占主导，生成更确定

-   **T = 1**：原始 softmax，不做任何调整

-   **T > 1**：logits 缩小，概率分布更平，各 Token 机会接近，生成更随机

物理上的类比：温度低，系统趋向低能态（确定）；温度高，系统各态都可能（随机）。这就是"温度"这个名字的来历。

### 2. 技术细节

#### 2.1 Temperature 对概率分布的影响

| Temperature | 分布形态 | 多样性 | 确定性 | 适用场景 |
| --- | --- | --- | --- | --- |
| 0（贪心） | 单点分布 | 无 | 最高 | 不推荐（重复严重） |
| 0.1-0.3 | 极集中 | 很低 | 很高 | 翻译、代码生成 |
| 0.5-0.7 | 正常 | 中 | 中 | 通用对话、推理 |
| 0.9-1.0 | 略平 | 较高 | 较低 | 创意写作 |
| 1.5+ | 很平 | 很高 | 很低 | 几乎不用（乱码风险） |

#### 2.2 Temperature 不能单独调

实际工程里，Temperature 通常和 Top-P、Frequency Penalty 配合用：

-   Temperature 控制分布的"胖瘦"

-   Top-P 控制从哪个"截止点"往前采样

-   Frequency Penalty 惩罚重复出现的 Token

光调 Temperature 不调 Top-P，有时候高温会让一些质量很差的 Token 也有机会被选到。推荐的组合：temperature=0.7, top\_p=0.9，是大多数场景的起点。

#### 2.3 什么时候用低温，什么时候用高温

-   **需要确定性答案**：事实问答、翻译、代码生成 → T 取 0.1-0.3，甚至 0

-   **需要多样性**：创意写作、头脑风暴、多样候选生成 → T 取 0.8-1.0

-   **Agent 工具调用**：一定要低温（0-0.1），工具名称和参数格式不能随机，否则 JSON 解析直接报错

## 代码示例

## 面试怎么答

> Temperature 通过在 softmax 之前除以 T 来调整概率分布。T < 1 时概率分布更尖，高概率 Token 更突出，生成确定；T > 1 时分布更平，各 Token 机会更均等，生成随机。
> 
> 工程里的使用建议：翻译、代码、事实问答这类"有标准答案"的任务用低温（0.1-0.3），创意写作、头脑风暴用高温（0.8-1.0）。Agent 里的工具调用一定要接近 0，格式出错解析直接失败。
> 
> Temperature 通常不单独用，要配合 Top-P 一起调。两者的分工：Temperature 决定分布"胖瘦"，Top-P 决定截取哪个尾部以上的 Token，组合起来比单独调效果更可控。

## 高频追问 & 加分点

-   **Q1：Temperature=0 和贪心搜索一样吗？** 在实现上是的——T → 0 时，softmax 退化成 argmax，只有最高概率的 Token 被选中。API 里通常不允许设 T=0，用 0.01 代替；也有的系统把 T=0 单独实现为贪心搜索。

-   **Q2：为什么说 Temperature 和熵有关？** Temperature 高时，概率分布的熵（不确定性）越大；Temperature 低时，熵越小。这和热力学里系统温度和熵的关系是一致的——这就是这个参数叫"温度"的数学来源。

-   **Q3：同样的 prompt，Temperature=0 每次输出一定相同吗？** 不一定。GPU 浮点运算有不确定性（并行计算的舍入误差），即使 T=0 在某些系统里也可能有极小概率的波动。真正想要确定性输出，还需要固定随机种子，并且在对应平台支持的情况下开启 deterministic 模式。

-   **Q4：多轮对话里 Temperature 怎么用？** 我自己的习惯是：对话回复保持中温（0.6-0.8）保证自然流畅；如果对话里需要生成代码或调用工具，那那个步骤单独降温。有些框架允许每轮单独设 Temperature，这个灵活性很有用。
