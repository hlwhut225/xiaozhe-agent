---
title: "2. 什么是离线评估?"
source: https://www.yuque.com/u28128023/mk3u4m/aag3o2xsqpi5g1ig
exported_at: 2026-07-27
---

# 2. 什么是离线评估?

## 知识点讲解

### 1. 核心概念

离线评估就是在系统上线之前，用一套提前准备好的静态测试集来跑分的过程。测试集固定，可以反复跑，每个版本的结果都可以对比——这是它最大的优势。

打个比方：离线评估是运动员在训练场练习，在线评估是正式比赛。训练场的成绩不代表比赛成绩，但没有训练就上场，大概率会翻车。

离线评估的五个关键特征：

1.  **静态性**：测试集不动，适合版本对比

1.  **低成本**：不需要真实用户，按需随时跑

1.  **可控性**：测试条件精确可控

1.  **快反馈**：通常分钟级出结果

1.  **局限性**：无法覆盖真实场景的长尾分布

### 2. 技术细节

#### 2.1 离线评估的技术栈

| 组件 | 作用 | 常见工具 |
| --- | --- | --- |
| 基准数据集 | 问题 + 预期答案 | AgentBench、自建集 |
| 评估指标 | 量化打分 | EM、F1、Task Success Rate |
| 执行引擎 | 跑 Agent 收结果 | LangChain Evaluators、自写 runner |
| 对比分析 | 版本间 diff | 统计检验、可视化 dashboard |

#### 2.2 构建离线评估集的步骤

这里最容易踩的坑是"采集完数据就标注，标注完就用"——跳过了质量控制那步，结果测试集本身就有问题。

**正确流程：**

1.  **需求分析**：先定要评估哪些场景和能力，再决定数据从哪来

1.  **数据采集**：从用户日志、专家构造、公开数据集里采，原始量是最终规模的 2-3 倍

1.  **分类标注**：按能力类型和难度打标签，附上预期答案和允许的工具调用路径

1.  **质量控制**：多人标注，计算一致性（Cohen's Kappa > 0.8 才算过关），有争议的找专家裁决

1.  **版本管理**：划分 dev/val/test 三个集，绝对不要在 test 集上调参数

#### 2.3 两种测试集：公开 vs 自建

公开 benchmark（AgentBench、SWE-bench 之类的）的好处是能和业界横向对比，缺点是不贴合自己的业务。自建的贴合业务，但标注成本高，而且容易过拟合到团队自己的思维盲区。

实践里我一般两个都用：**公开的作基线，自建的作主评**。

## 代码示例

## 面试怎么答

> 离线评估就是在部署前用固定测试集跑分的过程。它的核心价值是成本低、可重复——同一套测试集可以跑一百遍，每个版本的结果都能横向对比，改了什么、效果变没变一眼看出来。这是开发迭代阶段最主要的质量工具。
> 
> 它的局限也很明显：测试集是人工构造的，不管多努力都覆盖不了真实场景的长尾。我一般的判断标准是：离线评估通过了，说明系统没有明显缺陷；但离线评估好不代表用户体验好——这两个之间永远有 gap，需要在线评估去填。
> 
> 构建测试集有几个细节容易被忽略：一是质量控制，多人标注并算一致性（Cohen's Kappa 建议 > 0.8），有争议的题要专家定夺；二是集合要分层，dev 集调参数，test 集只用来最终汇报，别让 test 集被"看过"；三是定期更新，把在线暴露的失败案例补进来，测试集是个活的东西。

## 高频追问 & 加分点

-   **Q1：离线评估集多大才够？** 100-200 个样本能跑出初步指标。关键业务场景建议 1000+ 确保统计显著性。更重要的是覆盖均衡，简单/中等/困难三档都要有，别让测试集被简单题主导。

-   **Q2：怎么防止过拟合到测试集？** 三招：保持 dev/test 的严格隔离，只在 dev 上调参数；定期引入新场景更新测试集；重要的是，离线提升了要去在线验证，如果在线没有同步改善，说明在离线测试集上过拟合了。

-   **Q3：离线好但在线差，怎么排查？** 通常是测试集代表性不足。拿在线的失败案例做分析，看它们在测试集里有没有对应的题型——如果没有，就把这类题补进测试集。同时检查评估指标的设计有没有偏差，比如开放域题用了精确匹配，本来就会低估实际水平。

-   **Q4：公开 benchmark 和自建 benchmark 怎么选？** 不是选一个，而是两个都用各自干各自的事。公开 benchmark 用来和业界横向对比，确认自己不在某个能力维度上严重落后；自建的才是真正评估系统在你的业务场景上能不能打。
