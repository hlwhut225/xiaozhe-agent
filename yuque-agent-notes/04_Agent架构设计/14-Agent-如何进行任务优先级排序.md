---
title: "14. Agent 如何进行任务优先级排序？"
source: https://www.yuque.com/u28128023/mk3u4m/ac8262vaigkdsugc
exported_at: 2026-07-27
---

# 14. Agent 如何进行任务优先级排序？

## 知识点讲解

### 1. 核心概念

Agent 在同时面对多个任务或子任务时，不可能所有事情一起做，需要决定"先做哪个"。优先级排序就是这个决策机制。

注意这里有两种场景：

-   **多任务调度**：Agent 服务多个用户或任务，决定资源分配顺序

-   **子任务排序**：执行一个复杂任务时，内部子任务的执行顺序

两种场景的底层逻辑是一样的——给任务打分、按分数排序、按顺序执行。

优先级通常由四个维度决定：

| 维度 | 含义 | 权重参考 |
| --- | --- | --- |
| 紧急性（Urgency） | 截止时间有多近 | 40% |
| 重要性（Importance） | 对目标的贡献度 | 30% |
| 依赖权重（Dependency） | 有多少后续任务依赖它 | 20% |
| 成本（Effort） | 完成的代价（token/时间） | 10% |

### 2. 技术细节

#### 2.1 静态 vs 动态优先级

**静态优先级**：创建时确定，执行过程中不变。简单，适合任务之间相对独立的场景。

**动态优先级**：随时间和环境变化。截止时间越近优先级自动升高；某个任务被其他 Agent 阻塞了，依赖它的任务优先级也相应调整。

生产里通常用动态优先级，因为任务执行过程中信息会更新。

#### 2.2 依赖关系的处理

有依赖的任务不能乱序执行。先建 DAG（有向无环图），拓扑排序后再按优先级在每个"可执行层"内排序。

每批内部再按优先级分分数，高分先执行。

#### 2.3 避免优先级饥饿

低优先级任务如果一直被高优先级插队，可能永远不会被执行（饥饿）。解决方案：**aging 机制**——任务等待时间越长，优先级自动升一点，确保所有任务最终都能被执行。

## 代码示例

**任务优先级计算与排队**

**执行调度器**

**LLM 辅助的优先级评估（当规则不够时）**

## 面试怎么答

> Agent 的优先级排序是有限资源下的必要决策——不可能同时做所有事，必须排顺序。核心是四个维度：紧急性（截止时间）、重要性（对目标的贡献）、依赖权重（有多少后续任务依赖它）、执行成本（token 和时间）。加权组合成一个分数，按分数排序执行。
> 
> 有两个工程细节容易被忽略：一是依赖关系，有依赖的任务不能乱序，先建 DAG 拓扑排序，再在每批可执行任务里按优先级排；二是优先级饥饿，低优先级任务如果一直被插队可能永远不执行，需要 aging 机制——等待时间越长自动加一点优先级加成。
> 
> 纯规则算不够精确的时候，可以让 LLM 参与评估——给一批任务和总目标，让它评估每个任务对目标的贡献度，作为重要性维度的输入。规则算紧急性，LLM 算重要性，两者结合比任何一个单独用都好。

## 高频追问 & 加分点

-   **Q1：优先级权重怎么定？** 没有通用答案，取决于业务。时间敏感型（实时客服）紧急性权重要高；质量敏感型（医疗决策）重要性权重要高；成本敏感型可以多给成本维度的权重。初始值可以拍，然后根据任务完成率和用户反馈迭代调整——本质上是个超参数优化问题。

-   **Q2：任务截止时间 Agent 不知道怎么办？** 两个方法：一是让用户显式设置（"这个任务需要今天内完成"）；二是 LLM 根据任务描述估算（"整理季度报告"通常是非紧急的，"立刻回复客户投诉"是紧急的）。没有截止时间的任务默认紧急性为 0，靠重要性和依赖权重排序。

-   **Q3：任务执行中途可以调整优先级吗？** 可以。动态优先级的核心价值就在这——新任务进来、老任务截止时间更新、某个任务失败影响了依赖链，都可以触发重新排序。用一个 dirty 标志标记需要重排，下次调度时重新计算。

-   **Q4：多 Agent 场景下优先级怎么协调？** Orchestrator 负责全局优先级，Worker 只负责执行自己队列里的任务。Orchestrator 把任务分配给 Worker 时，同时传递优先级信息。如果多个 Worker 竞争同一个资源（比如数据库连接），用分布式队列保证高优先级任务先拿到资源。

-   **Q5：优先级排序影响用户体验的场景怎么处理？** 对用户可见的任务（比如用户发起的请求）优先级要高于系统内部的后台任务；同级别的用户请求要避免某个用户长期等待（公平性）。可以按用户维度分桶，每个桶内部按优先级排，桶之间轮转执行——类似 Linux 的 CFS 调度思路。
